<center>
<h4 class="sub-header">
<%= @photo.name %> | <%= @photo.category %>
</h4>

  <%= image_tag @photo.image.url(:content), class: 'img-responsive' %>

<div class='row col-md-8 col-md-offset-2'>
	<div class="text-center links">
		<% if current_user %>
		<%= link_to 'Edit', edit_subpage_path(@subpage) %> |
		<% end %>
		<%= link_to 'Back', url_for(:back) %>
	</div>
</div>
</center>