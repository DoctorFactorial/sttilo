<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @subpage.name %>
</p>

<p>
  <strong>Content:</strong>
  <%= raw RedCloth.new(@subpage.content).to_html %>
</p>

<%= image_tag @subpage.image.url(:original), class: 'img-responsive' %>

 <%= link_to 'Edit', edit_subpage_path(@subpage) %> |
 <%= link_to 'Delete', @subpage, :data => { :confirm => 'Are you sure?' }, :method => :delete %> |
 <%= link_to 'Back', subpages_path %>